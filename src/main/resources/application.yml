# Conversion to YAML from Properties formar report
# Warnings:
# - The yaml file had comments which are lost in the refactoring!
spring:
  datasource:
    url: jdbc:oracle:thin:@//localhost:1539/ORCLPDB
    username: scott
    password: tiger
    driver-class-name: oracle.jdbc.OracleDriver
